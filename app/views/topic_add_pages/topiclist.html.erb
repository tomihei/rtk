  </div>
  </div>
  </div>
  </nav>
  <div class="container-fluid">
  <div class="row">
  <div class="col-xs-12 col-md-12 col-sm-12">
  <ul>
  <% @list.each_key do |key| %>
    <li><%= key %>:
    <%= link_to "#{@list["#{key}"][0]}",:controller => "chat", :action => "index", :id => key%>
    :<%= @list["#{key}"][1] %>:
    <%= @list["#{key}"][2]  %>
    </li>
  <% end %>
  </ul>
  </div>


   </div>
   </div>

